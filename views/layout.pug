doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  body.container 
    nav(class="navbar navbar-expand-lg navbar-light bg-light")  
      div(class="collapse navbar-collapse" )
        ul(class="navbar-nav") 

          li(class="nav-item")
            a(class=(page === "index" ? "nav-link active": "nav-link") href="/") Home

          if !locals.user
            li(class="nav-item")
              a(class=(page === "signin" ? "nav-link active": "nav-link") href="/signin") Sign in 

            li(class="nav-item") 
              a(class=(page === "signup" ? "nav-link active": "nav-link") href="/signup") Sing up 

          if locals.user
            if !locals.user.isVIP
              li(class="nav-item") 
                a(class=(page === "VIP-application" ? "nav-link active": "nav-link") href="/users/VIP-application") VIP application

            if locals.user.isVIP
              li(class="nav-item") 
                a(class=(page === "new-post" ? "nav-link active": "nav-link") href="/message/new-post") New message

            li(class="nav-item") 
              a(class="nav-link active" href="/logout") Logout 
    
    div(class="container-fluid")
      block content
